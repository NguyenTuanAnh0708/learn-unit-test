import { screen, render } from "@testing-library/react";
import user from '@testing-library/user-event';
import UserForm from './UserForm';

test('Render 2 buttons', () => {
    // render component 

    // manipulate the components or find components on it
    // ASSERTION - MAKE SURE
})